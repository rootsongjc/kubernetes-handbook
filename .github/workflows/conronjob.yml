name: update-algolia-index

on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  build:
    name: Trigger Algolia Update
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v2

      - name: Update Algolia
        # Hit the webhook endpoint to rebuild  from the "main" branch
        run: update-algolia.sh
